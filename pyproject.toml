[project]
name = "claude-usage-menubar"
version = "0.1.0"
description = "macOS menu bar app displaying Claude Code local usage totals"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
